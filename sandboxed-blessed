#!/bin/bash
# Medieval Life Simulation - Blessed UI Edition
# Beautiful terminal graphics powered by blessed

cd "$(dirname "$0")"

# Check if node_modules exists
if [ ! -d "node_modules" ]; then
    echo "Installing dependencies..."
    npm install
fi

# Run with blessed UI
node src/main-blessed.js "$@"
